var CSRF_TOKEN = $('html').data('csrf-token');
var GIT_REV = $('html').data('rev');
var LANGUAGE = $('html').attr('lang');

var USER_ID = +$('html').data('user');
var USER_IS_AUTHENTICATED = (USER_ID > 0);

var INTERNSHIP = 17;
